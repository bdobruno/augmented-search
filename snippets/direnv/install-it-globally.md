# Explains how direnv is installed and configured


| step                 | task                                           |
| -------------------- | ---------------------------------------------- |
| install library      | `brew install direnv`                          |
| hook it in the shell | `echo 'eval "$(direnv hook zsh)"' >> ~/.zshrc` |
